# ============================================
# Community Arctic Map - Environment Variables
# ============================================
# This file serves as a template for environment variables
# Copy this file to create your local .env files:
#   - backend/.env (for backend services)
#   - frontend/.env (for frontend build)

# ============================================
# MAPBOX CONFIGURATION (Required)
# ============================================
# Mapbox access token for map rendering
# Get your token at: https://account.mapbox.com/access-tokens/
VITE_MAPBOX_ACCESS_TOKEN=your_mapbox_token_here

# ============================================
# GOOGLE SHEETS CONFIGURATION (Required)
# ============================================
# Google Sheet ID for layer hierarchy and metadata
# Format: From URL https://docs.google.com/spreadsheets/d/{SHEET_ID}/edit#gid={GID}
GOOGLE_SHEET_ID=your_google_sheet_id_here
GOOGLE_SHEET_GID=your_google_sheet_gid_here

# ============================================
# API ENDPOINTS (Production Only)
# ============================================
# These are used during frontend build for production deployments
# Leave empty for local development (will use localhost)
VITE_BACKEND_API_URL=
VITE_DOWNLOAD_API_URL=

# ============================================
# DEPLOYMENT CONFIGURATION (Cloud Run)
# ============================================
# Port for the backend service (Cloud Run provides this automatically)
PORT=8080

# ============================================
# FILE PATHS (Cloud Run Deployment)
# ============================================
# Paths for large data files (use Cloud Storage in production)
CPAD_SQLITE_PATH=/app/data/cpad.sqlite
METADATA_HTML_PATH=/app/data/metadata.html
ZIPPED_SHAPEFILES_PATH=/app/data/zipped_shapefiles
